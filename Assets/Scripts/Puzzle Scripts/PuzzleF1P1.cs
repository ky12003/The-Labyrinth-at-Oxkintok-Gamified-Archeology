using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// mini notes
// deactivate puzzle (change layer to undefined) after puzzle is done

public class PuzzleF1P1 : MonoBehaviour
{
    // UI objects
    // PLACEHOLDER (JUST COMMENTS FOR NOW)
    /*
    [SerializeField] Image numberOne
    [SerializeField] Image numberFour
    [SerializeField] Image
    */

    // Public variables
    public bool puzzleIsDone = false; // for noting down if the puzzle is complete

    // Private variables
    int currStep = 1; // current step
    int finStep = 5; // final step
    

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
